# Model Configuration
models:
  ner:
    name: "dslim/bert-base-NER"
    cache_dir: "data/models/ner"
    device: "cuda"  # Change to "cuda" if GPU available
  
  sentiment:
    name: "ProsusAI/finbert"
    cache_dir: "data/models/sentiment"
    device: "cuda"
  
  qa:
    name: "deepset/roberta-base-squad2"
    cache_dir: "data/models/qa"
    device: "cuda"
  
  summarization:
    name: "facebook/bart-large-cnn"
    cache_dir: "data/models/summarization"
    device: "cuda"
  
  embeddings:
    name: "sentence-transformers/all-MiniLM-L6-v2"
    cache_dir: "data/models/embeddings"
    device: "cuda"

  # Fine-tuned models (local paths)
  ner_finetuned:
    name: "models/financial_ner"
    cache_dir: null  # Not needed for local models
    device: "cuda"
    type: "local"
    description: "Fine-tuned NER for financial entities"
  
  sentiment_finetuned:
    name: "models/finbert_custom"
    cache_dir: null
    device: "cuda"
    type: "local"
    description: "Fine-tuned FinBERT for financial sentiment"

# Model parameters
parameters:
  max_length: 512
  batch_size: 8
  num_beams: 4
  temperature: 0.7
